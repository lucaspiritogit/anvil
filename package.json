{
  "name": "anvil",
  "version": "1.2.10",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "description": "Desktop control plane for running coding agents in parallel Git worktrees and reviewing their diffs.",
  "main": "./out/main/index.js",
  "author": "lucasignaciopirito@gmail.com",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24.15.0"
  },
  "scripts": {
    "test:e2e": "playwright test",
    "dev": "npm run build:server && node scripts/dev.cjs",
    "demo": "node scripts/capture-demo.mjs",
    "build": "npm run typecheck && npm run build:app",
    "build:app": "npm run build:desktop",
    "typecheck": "tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.web.json",
    "db:generate": "drizzle-kit generate --config apps/server/drizzle.config.ts",
    "db:migrate": "node scripts/maintenance.cjs migrate",
    "memory:generate": "drizzle-kit generate --config apps/server/drizzle.memory.config.ts",
    "memory:migrate": "drizzle-kit migrate --config apps/server/drizzle.memory.config.ts",
    "memory:inspect": "node scripts/maintenance.cjs memory",
    "db:drop": "node scripts/maintenance.cjs drop",
    "db:reset": "npm run db:drop && npm run db:migrate",
    "start": "npm run build:app && electron-vite preview --config apps/desktop/electron.vite.config.ts",
    "pack": "npm run build && electron-builder --win --dir",
    "dist": "npm run build && electron-builder --win",
    "release": "node scripts/release.mjs",
    "pack:mac": "npm run build && electron-builder --mac --dir",
    "dist:mac": "npm run build && electron-builder --mac dmg",
    "rebuild": "electron-builder install-app-deps",
    "typecheck:tests": "tsc --noEmit -p tsconfig.test.json",
    "test:node-pty": "node scripts/tests/verify-node-pty.mjs",
    "test:unit": "vitest run --project unit",
    "test:integration": "vitest run --project integration",
    "test": "vitest run",
    "build:web": "npm run build --workspace @anvil/web",
    "build:server": "npm run build:web && npm run build --workspace @anvil/server",
    "build:desktop": "npm run build:server && npm run build --workspace @anvil/desktop",
    "package:server": "npm run build:server && node scripts/package-server.mjs",
    "serve": "npm run build:server && node out/server/index.js --headless",
    "serve:lan": "npm run build:server && node out/server/index.js --headless --lan",
    "serve:tailscale": "npm run build:server && node out/server/index.js --headless --tailscale"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^1.4.0",
    "@electric-sql/pglite": "^0.5.8",
    "@electric-sql/pglite-pgvector": "^0.0.9",
    "@hono/node-server": "^2.1.1",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@opencode-ai/sdk": "1.18.30",
    "argon2": "^0.45.1",
    "drizzle-orm": "^1.0.0-rc.4",
    "hono": "^4.13.7",
    "node-pty": "^1.1.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@pierre/diffs": "^1.4.0",
    "@playwright/test": "^1.63.0",
    "@tailwindcss/vite": "^4.3.3",
    "@types/d3-scale": "^4.0.9",
    "@types/d3-shape": "^3.2.0",
    "@types/node": "^24.13.4",
    "@types/pg": "^8.23.1",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "@vitejs/plugin-react": "^6.1.1",
    "drizzle-kit": "^1.0.0-rc.4",
    "electron": "^44.3.0",
    "electron-builder": "^26.15.3",
    "electron-vite": "^6.0.0-beta.1",
    "esbuild": "^0.28.2",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.9.3",
    "vite": "^8.2.2",
    "vitest": "^5.0.0",
    "zustand": "^5.0.2"
  }
}
